"""
Update Module
"""


def main() -> None:

    # pull down the latest release,

    # compare version to app.json

    # download and unzip files of the release if needed into a new directory, bin (.gitignored)

    # walk through each subdirectory of bin
    # don't touch conf/ -> except update app.json (need the new version number)
    # /utils/
    # /cls/pltfm/ -> update all .py files

    #






    return


